import { IDatabaseDriver } from '@common/drivers';
import { ILogger } from './logger';

export type DatabaseDriver = IDatabaseDriver;

export type Logger = ILogger;

export type ModulePrefix = string | undefined;
